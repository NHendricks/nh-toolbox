# Backend CLI Application

This a s a cmdline app.

It is used my the process project which is called by the ui. All commands can be separately tested in the ui.

This help the AI to do simple separate tasks while implementing the functions.

And it keeps the code nice.
