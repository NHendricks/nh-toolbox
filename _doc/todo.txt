commander:
- fix no ctrl+1 dialog when only 1 drive is there (mac) so we cant set favorites
- build asar for better performance abd less files to transport / scan at runtime
- progress dialog also when copying directory
- memorize directories and enable backward history navigation
- fix icon app mac
- how does file filter work on mac?
- remove console outputs when zipping
- fonts / colors
  toolbar-title: rgb(36 199 251)
  ...
- prefs export / import
- support \\paths (Windows only) ? Or better map to drive … .
- cancel all fileops when closing compare dialog

other:
- build app for linux
- integrate more cmdline-Tools
  - data search dialog (like my proficash search dialog but more general approach)
  - dirsorter for jpegs
  - mp3metatagsetter for mp3s
- music player (IPOD-like see www.nilshendricks.de>widgets)